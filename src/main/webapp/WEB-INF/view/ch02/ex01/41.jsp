<%@ page language='java' contentType='text/html; charset=utf-8' pageEncoding='utf-8'%>
${user.userName} / ${user.age}

<!--
 
user.jsp servlet관점에서는 Model을 Attribute이다.
user.jsp는 attribute를 받은거다.
받은 attribute의 name이 user이다.
그리고 최한석, 11은 value가 되는거다.

변수명 칼럼명 지을때
name , id, date, number는 단독으로 쓰지않고
userName, laborerName 같이 붙여서 쓴다.

 -->
 
 
 